use std::fs;

mod io {


    // fn read_to_strings(fileName: &str) -> Vec<String> {
    //     let contents = fs::read_to_string(filename).expect("Something went wrong reading the file");
    //     contents.split_terminator(pat: P)

    // }
}